spring:
  datasource:
    url: jdbc:mysql://localhost:3306/skillgram
    username: your_db_user
    password: your_db_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: ${JWT_SECRET:random_generated_secret}
  expirationMs: 3600000
server:
  port: 8080
